<%- include('../partials/head', { title: typeof title !== 'undefined' ? title : '錯誤' }) %>
<body class="min-h-screen bg-slate-950 text-slate-100 font-sans">
  <main class="mx-auto max-w-lg px-4 py-20 text-center">
    <h1 class="text-2xl font-bold text-amber-400"><%= typeof title !== 'undefined' ? title : '錯誤' %></h1>
    <p class="mt-4 text-slate-300"><%= typeof message !== 'undefined' ? message : '' %></p>
    <p class="mt-8"><a class="text-emerald-400 underline" href="/">返回首頁</a></p>
  </main>
</body>
</html>
